{
  "name": "valid-gtin",
  "version": "2.1.1",
  "type": "module",
  "scripts": {
    "coverage": "vitest run --coverage",
    "prepublishOnly": "npm run lint && npm run test && npm run minify",
    "lint": "eslint",
    "minify": "terser src/index.js -o dist/index.js -c -m",
    "presize": "npm run minify",
    "size": "size-limit",
    "pretest": "npm run minify",
    "test": "vitest run"
  },
  "author": "0xflotus",
  "keywords": [
    "gtin",
    "validate"
  ],
  "repository": "github:0xflotus/valid-gtin",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@size-limit/preset-small-lib": "14.0.0",
    "@vitest/coverage-v8": "5.0.1",
    "eslint": "10.11.0",
    "fast-check": "4.10.2",
    "size-limit": "14.0.0",
    "terser": "5.51.2",
    "vitest": "5.0.1"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "fsevents@2.3.3": true
  }
}
